[package]
name = "demo"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = "1.0.196"
tokio = { version = "1.36.0", features = ["full"] }
local-dep = { path = "../other" }

[dev-dependencies]
criterion = "0.5.1"

[build-dependencies]
cc = "1.0.83"
